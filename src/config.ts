export const config = {
  // If true, moving the mouse to any point in FAILSAFE_POINTS will throw FailSafeException
  FAILSAFE: false,
  
  // Array of points to check for failsafe
  FAILSAFE_POINTS: [[0, 0]] as [number, number][]
}
